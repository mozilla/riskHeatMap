import os

activate_this = '<%= @virtualenv_dir %>/bin/activate_this.py'
execfile(activate_this, dict(__file__=activate_this))

os.environ['APP_NAME'] = '<%= @app_name %>'

from flaskoktaapp import app as application
